import XCTest
@testable import raspberry_pi

final class raspberry_piTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(raspberry_pi().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
